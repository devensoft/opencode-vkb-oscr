---
description: Run full OpenSpec Change Request orchestration workflow
agent: vibe-orchestrator
---

Orchestrate the following OpenSpec change request(s) through the full OSCR workflow (intake → plan → execute → finalize): $ARGUMENTS

If no arguments provided, orchestrate all active OpenSpec changes.

Load the oscr-intake skill to begin.
